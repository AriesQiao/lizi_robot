#install Joystick driver

sudo apt-get install ros-hydro-joystick-drivers

rosdep install joy

rospack find joy

ls /dev/input/

sudo jstest /dev/input/js0

sudo chmod a+rw /dev/input/js0 rosparam set joy_node/dev "/dev/input/js0"

rosparam set joy_node/dev "/dev/input/js0"

rosrun joy joy_node

rosrun joy_cam joy_cam.py
